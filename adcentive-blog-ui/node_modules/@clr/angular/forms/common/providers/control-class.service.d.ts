export declare class ControlClassService {
    className: string;
    controlClass(invalid?: boolean, grid?: boolean, additional?: string): string;
    initControlClass(renderer: any, element: HTMLElement): void;
}
